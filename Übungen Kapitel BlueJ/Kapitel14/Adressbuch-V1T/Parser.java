import java.util.Scanner;/** * Eine Klasse, die Eingaben vom Benutzer entgegen nimmt. * Die Eingaben werden in 'liefereBefehl' nach gültigen * Befehlen gefiltert. * * @author  David J. Barnes und Michael Kölling * @version 2016.02.29 */public class Parser {    // Die gültigen Befehlswörter.    private Befehlswoerter befehlswoerter;    private Scanner scanner;    public Parser()     {        befehlswoerter = new Befehlswoerter();        scanner = new Scanner(System.in);    }    /**     * Lies den nächsten Benutzerbefehl ein.     * Der gelieferte Befehl ist gültig.     * @return  einen gültigen Befehl     */    public String liefereBefehl()     {        String befehl = null;        do {            // Eingabeaufforderung ausgeben            System.out.print("> ");                        String wort = scanner.next();            // Den Rest der Zeile verwerfen.            zeileEinlesen();            if(befehlswoerter.istBefehl(wort)) {                befehl = wort;            }            else{                System.out.println("Unbekannter Befehl: " + wort);                System.out.print("Gültige Befehle sind: ");                befehlswoerter.alleAusgeben();            }        } while(befehl == null);            return befehl;    }        /**     * Gib eine Liste der gültigen Befehlswörter     * auf der Konsole aus.     */    public void zeigeBefehle()    {        befehlswoerter.alleAusgeben();    }    /**     * @return  eine Zeile Text vom Benutzer     */    public String zeileEinlesen()    {        return scanner.nextLine();    }}